fy-agility_story() {
    yt --transcript "$1" | fabric -sp agility_story | nvim
}
fy-ai() {
    yt --transcript "$1" | fabric -sp ai | nvim
}
fy-analyze_answers() {
    yt --transcript "$1" | fabric -sp analyze_answers | nvim
}
fy-analyze_cfp_submission() {
    yt --transcript "$1" | fabric -sp analyze_cfp_submission | nvim
}
fy-analyze_claims() {
    yt --transcript "$1" | fabric -sp analyze_claims | nvim
}
fy-analyze_comments() {
    yt --transcript "$1" | fabric -sp analyze_comments | nvim
}
fy-analyze_debate() {
    yt --transcript "$1" | fabric -sp analyze_debate | nvim
}
fy-analyze_email_headers() {
    yt --transcript "$1" | fabric -sp analyze_email_headers | nvim
}
fy-analyze_incident() {
    yt --transcript "$1" | fabric -sp analyze_incident | nvim
}
fy-analyze_interviewer_techniques() {
    yt --transcript "$1" | fabric -sp analyze_interviewer_techniques | nvim
}
fy-analyze_logs() {
    yt --transcript "$1" | fabric -sp analyze_logs | nvim
}
fy-analyze_malware() {
    yt --transcript "$1" | fabric -sp analyze_malware | nvim
}
fy-analyze_paper() {
    yt --transcript "$1" | fabric -sp analyze_paper | nvim
}
fy-analyze_patent() {
    yt --transcript "$1" | fabric -sp analyze_patent | nvim
}
fy-analyze_personality() {
    yt --transcript "$1" | fabric -sp analyze_personality | nvim
}
fy-analyze_presentation() {
    yt --transcript "$1" | fabric -sp analyze_presentation | nvim
}
fy-analyze_product_feedback() {
    yt --transcript "$1" | fabric -sp analyze_product_feedback | nvim
}
fy-analyze_prose() {
    yt --transcript "$1" | fabric -sp analyze_prose | nvim
}
fy-analyze_prose_json() {
    yt --transcript "$1" | fabric -sp analyze_prose_json | nvim
}
fy-analyze_prose_pinker() {
    yt --transcript "$1" | fabric -sp analyze_prose_pinker | nvim
}
fy-analyze_sales_call() {
    yt --transcript "$1" | fabric -sp analyze_sales_call | nvim
}
fy-analyze_spiritual_text() {
    yt --transcript "$1" | fabric -sp analyze_spiritual_text | nvim
}
fy-analyze_tech_impact() {
    yt --transcript "$1" | fabric -sp analyze_tech_impact | nvim
}
fy-analyze_threat_report() {
    yt --transcript "$1" | fabric -sp analyze_threat_report | nvim
}
fy-analyze_threat_report_trends() {
    yt --transcript "$1" | fabric -sp analyze_threat_report_trends | nvim
}
fy-answer_interview_question() {
    yt --transcript "$1" | fabric -sp answer_interview_question | nvim
}
fy-ask_secure_by_design_questions() {
    yt --transcript "$1" | fabric -sp ask_secure_by_design_questions | nvim
}
fy-capture_thinkers_work() {
    yt --transcript "$1" | fabric -sp capture_thinkers_work | nvim
}
fy-check_agreement() {
    yt --transcript "$1" | fabric -sp check_agreement | nvim
}
fy-clean_text() {
    yt --transcript "$1" | fabric -sp clean_text | nvim
}
fy-coding_master() {
    yt --transcript "$1" | fabric -sp coding_master | nvim
}
fy-compare_and_contrast() {
    yt --transcript "$1" | fabric -sp compare_and_contrast | nvim
}
fy-create_5_sentence_summary() {
    yt --transcript "$1" | fabric -sp create_5_sentence_summary | nvim
}
fy-create_academic_paper() {
    yt --transcript "$1" | fabric -sp create_academic_paper | nvim
}
fy-create_ai_jobs_analysis() {
    yt --transcript "$1" | fabric -sp create_ai_jobs_analysis | nvim
}
fy-create_aphorisms() {
    yt --transcript "$1" | fabric -sp create_aphorisms | nvim
}
fy-create_art_prompt() {
    yt --transcript "$1" | fabric -sp create_art_prompt | nvim
}
fy-create_better_frame() {
    yt --transcript "$1" | fabric -sp create_better_frame | nvim
}
fy-create_coding_project() {
    yt --transcript "$1" | fabric -sp create_coding_project | nvim
}
fy-create_command() {
    yt --transcript "$1" | fabric -sp create_command | nvim
}
fy-create_cyber_summary() {
    yt --transcript "$1" | fabric -sp create_cyber_summary | nvim
}
fy-create_formal_email() {
    yt --transcript "$1" | fabric -sp create_formal_email | nvim
}
fy-create_git_diff_commit() {
    yt --transcript "$1" | fabric -sp create_git_diff_commit | nvim
}
fy-create_graph_from_input() {
    yt --transcript "$1" | fabric -sp create_graph_from_input | nvim
}
fy-create_hormozi_offer() {
    yt --transcript "$1" | fabric -sp create_hormozi_offer | nvim
}
fy-create_idea_compass() {
    yt --transcript "$1" | fabric -sp create_idea_compass | nvim
}
fy-create_investigation_visualization() {
    yt --transcript "$1" | fabric -sp create_investigation_visualization | nvim
}
fy-create_keynote() {
    yt --transcript "$1" | fabric -sp create_keynote | nvim
}
fy-create_logo() {
    yt --transcript "$1" | fabric -sp create_logo | nvim
}
fy-create_markmap_visualization() {
    yt --transcript "$1" | fabric -sp create_markmap_visualization | nvim
}
fy-create_mermaid_visualization() {
    yt --transcript "$1" | fabric -sp create_mermaid_visualization | nvim
}
fy-create_mermaid_visualization_for_github() {
    yt --transcript "$1" | fabric -sp create_mermaid_visualization_for_github | nvim
}
fy-create_micro_summary() {
    yt --transcript "$1" | fabric -sp create_micro_summary | nvim
}
fy-create_network_threat_landscape() {
    yt --transcript "$1" | fabric -sp create_network_threat_landscape | nvim
}
fy-create_npc() {
    yt --transcript "$1" | fabric -sp create_npc | nvim
}
fy-create_pattern() {
    yt --transcript "$1" | fabric -sp create_pattern | nvim
}
fy-create_quiz() {
    yt --transcript "$1" | fabric -sp create_quiz | nvim
}
fy-create_reading_plan() {
    yt --transcript "$1" | fabric -sp create_reading_plan | nvim
}
fy-create_recursive_outline() {
    yt --transcript "$1" | fabric -sp create_recursive_outline | nvim
}
fy-create_report_finding() {
    yt --transcript "$1" | fabric -sp create_report_finding | nvim
}
fy-create_rpg_summary() {
    yt --transcript "$1" | fabric -sp create_rpg_summary | nvim
}
fy-create_security_update() {
    yt --transcript "$1" | fabric -sp create_security_update | nvim
}
fy-create_show_intro() {
    yt --transcript "$1" | fabric -sp create_show_intro | nvim
}
fy-create_sigma_rules() {
    yt --transcript "$1" | fabric -sp create_sigma_rules | nvim
}
fy-create_story_explanation() {
    yt --transcript "$1" | fabric -sp create_story_explanation | nvim
}
fy-create_stride_threat_model() {
    yt --transcript "$1" | fabric -sp create_stride_threat_model | nvim
}
fy-create_summary() {
    yt --transcript "$1" | fabric -sp create_summary | nvim
}
fy-create_tags() {
    yt --transcript "$1" | fabric -sp create_tags | nvim
}
fy-create_threat_scenarios() {
    yt --transcript "$1" | fabric -sp create_threat_scenarios | nvim
}
fy-create_ttrc_graph() {
    yt --transcript "$1" | fabric -sp create_ttrc_graph | nvim
}
fy-create_ttrc_narrative() {
    yt --transcript "$1" | fabric -sp create_ttrc_narrative | nvim
}
fy-create_upgrade_pack() {
    yt --transcript "$1" | fabric -sp create_upgrade_pack | nvim
}
fy-create_video_chapters() {
    yt --transcript "$1" | fabric -sp create_video_chapters | nvim
}
fy-create_visualization() {
    yt --transcript "$1" | fabric -sp create_visualization | nvim
}
fy-explain_code() {
    yt --transcript "$1" | fabric -sp explain_code | nvim
}
fy-explain_docs() {
    yt --transcript "$1" | fabric -sp explain_docs | nvim
}
fy-explain_math() {
    yt --transcript "$1" | fabric -sp explain_math | nvim
}
fy-explain_project() {
    yt --transcript "$1" | fabric -sp explain_project | nvim
}
fy-explain_terms() {
    yt --transcript "$1" | fabric -sp explain_terms | nvim
}
fy-export_data_as_csv() {
    yt --transcript "$1" | fabric -sp export_data_as_csv | nvim
}
fy-extract_algorithm_update_recommendations() {
    yt --transcript "$1" | fabric -sp extract_algorithm_update_recommendations | nvim
}
fy-extract_article_wisdom() {
    yt --transcript "$1" | fabric -sp extract_article_wisdom | nvim
}
fy-extract_book_ideas() {
    yt --transcript "$1" | fabric -sp extract_book_ideas | nvim
}
fy-extract_book_recommendations() {
    yt --transcript "$1" | fabric -sp extract_book_recommendations | nvim
}
fy-extract_business_ideas() {
    yt --transcript "$1" | fabric -sp extract_business_ideas | nvim
}
fy-extract_controversial_ideas() {
    yt --transcript "$1" | fabric -sp extract_controversial_ideas | nvim
}
fy-extract_ctf_writeup() {
    yt --transcript "$1" | fabric -sp extract_ctf_writeup | nvim
}
fy-extract_extraordinary_claims() {
    yt --transcript "$1" | fabric -sp extract_extraordinary_claims | nvim
}
fy-extract_ideas() {
    yt --transcript "$1" | fabric -sp extract_ideas | nvim
}
fy-extract_insights() {
    yt --transcript "$1" | fabric -sp extract_insights | nvim
}
fy-extract_insights_dm() {
    yt --transcript "$1" | fabric -sp extract_insights_dm | nvim
}
fy-extract_instructions() {
    yt --transcript "$1" | fabric -sp extract_instructions | nvim
}
fy-extract_jokes() {
    yt --transcript "$1" | fabric -sp extract_jokes | nvim
}
fy-extract_main_idea() {
    yt --transcript "$1" | fabric -sp extract_main_idea | nvim
}
fy-extract_patterns() {
    yt --transcript "$1" | fabric -sp extract_patterns | nvim
}
fy-extract_poc() {
    yt --transcript "$1" | fabric -sp extract_poc | nvim
}
fy-extract_predictions() {
    yt --transcript "$1" | fabric -sp extract_predictions | nvim
}
fy-extract_primary_problem() {
    yt --transcript "$1" | fabric -sp extract_primary_problem | nvim
}
fy-extract_questions() {
    yt --transcript "$1" | fabric -sp extract_questions | nvim
}
fy-extract_recommendations() {
    yt --transcript "$1" | fabric -sp extract_recommendations | nvim
}
fy-extract_references() {
    yt --transcript "$1" | fabric -sp extract_references | nvim
}
fy-extract_skills() {
    yt --transcript "$1" | fabric -sp extract_skills | nvim
}
fy-extract_song_meaning() {
    yt --transcript "$1" | fabric -sp extract_song_meaning | nvim
}
fy-extract_sponsors() {
    yt --transcript "$1" | fabric -sp extract_sponsors | nvim
}
fy-extract_videoid() {
    yt --transcript "$1" | fabric -sp extract_videoid | nvim
}
fy-extract_wisdom() {
    yt --transcript "$1" | fabric -sp extract_wisdom | nvim
}
fy-extract_wisdom_agents() {
    yt --transcript "$1" | fabric -sp extract_wisdom_agents | nvim
}
fy-extract_wisdom_dm() {
    yt --transcript "$1" | fabric -sp extract_wisdom_dm | nvim
}
fy-extract_wisdom_nometa() {
    yt --transcript "$1" | fabric -sp extract_wisdom_nometa | nvim
}
fy-find_hidden_message() {
    yt --transcript "$1" | fabric -sp find_hidden_message | nvim
}
fy-find_logical_fallacies() {
    yt --transcript "$1" | fabric -sp find_logical_fallacies | nvim
}
fy-get_wow_per_minute() {
    yt --transcript "$1" | fabric -sp get_wow_per_minute | nvim
}
fy-get_youtube_rss() {
    yt --transcript "$1" | fabric -sp get_youtube_rss | nvim
}
fy-improve_academic_writing() {
    yt --transcript "$1" | fabric -sp improve_academic_writing | nvim
}
fy-improve_prompt() {
    yt --transcript "$1" | fabric -sp improve_prompt | nvim
}
fy-improve_report_finding() {
    yt --transcript "$1" | fabric -sp improve_report_finding | nvim
}
fy-improve_writing() {
    yt --transcript "$1" | fabric -sp improve_writing | nvim
}
fy-label_and_rate() {
    yt --transcript "$1" | fabric -sp label_and_rate | nvim
}
fy-official_pattern_template() {
    yt --transcript "$1" | fabric -sp official_pattern_template | nvim
}
fy-provide_guidance() {
    yt --transcript "$1" | fabric -sp provide_guidance | nvim
}
fy-rate_ai_response() {
    yt --transcript "$1" | fabric -sp rate_ai_response | nvim
}
fy-rate_ai_result() {
    yt --transcript "$1" | fabric -sp rate_ai_result | nvim
}
fy-rate_content() {
    yt --transcript "$1" | fabric -sp rate_content | nvim
}
fy-rate_value() {
    yt --transcript "$1" | fabric -sp rate_value | nvim
}
fy-raw_query() {
    yt --transcript "$1" | fabric -sp raw_query | nvim
}
fy-raycast() {
    yt --transcript "$1" | fabric -sp raycast | nvim
}
fy-recommend_artists() {
    yt --transcript "$1" | fabric -sp recommend_artists | nvim
}
fy-recommend_pipeline_upgrades() {
    yt --transcript "$1" | fabric -sp recommend_pipeline_upgrades | nvim
}
fy-recommend_talkpanel_topics() {
    yt --transcript "$1" | fabric -sp recommend_talkpanel_topics | nvim
}
fy-show_fabric_options_markmap() {
    yt --transcript "$1" | fabric -sp show_fabric_options_markmap | nvim
}
fy-solve_with_cot() {
    yt --transcript "$1" | fabric -sp solve_with_cot | nvim
}
fy-suggest_pattern() {
    yt --transcript "$1" | fabric -sp suggest_pattern | nvim
}
fy-summarize() {
    yt --transcript "$1" | fabric -sp summarize | nvim
}
fy-summarize_debate() {
    yt --transcript "$1" | fabric -sp summarize_debate | nvim
}
fy-summarize_git_changes() {
    yt --transcript "$1" | fabric -sp summarize_git_changes | nvim
}
fy-summarize_git_diff() {
    yt --transcript "$1" | fabric -sp summarize_git_diff | nvim
}
fy-summarize_lecture() {
    yt --transcript "$1" | fabric -sp summarize_lecture | nvim
}
fy-summarize_legislation() {
    yt --transcript "$1" | fabric -sp summarize_legislation | nvim
}
fy-summarize_micro() {
    yt --transcript "$1" | fabric -sp summarize_micro | nvim
}
fy-summarize_newsletter() {
    yt --transcript "$1" | fabric -sp summarize_newsletter | nvim
}
fy-summarize_paper() {
    yt --transcript "$1" | fabric -sp summarize_paper | nvim
}
fy-summarize_prompt() {
    yt --transcript "$1" | fabric -sp summarize_prompt | nvim
}
fy-summarize_pull-requests() {
    yt --transcript "$1" | fabric -sp summarize_pull | nvim
}
fy-summarize_rpg_session() {
    yt --transcript "$1" | fabric -sp summarize_rpg_session | nvim
}
fy-to_flashcards() {
    yt --transcript "$1" | fabric -sp to_flashcards | nvim
}
fy-transcribe_minutes() {
    yt --transcript "$1" | fabric -sp transcribe_minutes | nvim
}
fy-tweet() {
    yt --transcript "$1" | fabric -sp tweet | nvim
}
fy-write_essay() {
    yt --transcript "$1" | fabric -sp write_essay | nvim
}
fy-write_hackerone_report() {
    yt --transcript "$1" | fabric -sp write_hackerone_report | nvim
}
fy-write_latex() {
    yt --transcript "$1" | fabric -sp write_latex | nvim
}
fy-write_micro_essay() {
    yt --transcript "$1" | fabric -sp write_micro_essay | nvim
}
fy-write_nuclei_template_rule() {
    yt --transcript "$1" | fabric -sp write_nuclei_template_rule | nvim
}
fy-write_pull-request() {
    yt --transcript "$1" | fabric -sp write_pull | nvim
}
fy-write_semgrep_rule() {
    yt --transcript "$1" | fabric -sp write_semgrep_rule | nvim
}

# ------------------------------------------------------------------------------

ft-agility_story() {
    echo "$*" | fabric -sp agility_story | nvim;
}
ft-ai() {
    echo "$*" | fabric -sp ai | nvim;
}
ft-analyze_answers() {
    echo "$*" | fabric -sp analyze_answers | nvim;
}
ft-analyze_cfp_submission() {
    echo "$*" | fabric -sp analyze_cfp_submission | nvim;
}
ft-analyze_claims() {
    echo "$*" | fabric -sp analyze_claims | nvim;
}
ft-analyze_comments() {
    echo "$*" | fabric -sp analyze_comments | nvim;
}
ft-analyze_debate() {
    echo "$*" | fabric -sp analyze_debate | nvim;
}
ft-analyze_email_headers() {
    echo "$*" | fabric -sp analyze_email_headers | nvim;
}
ft-analyze_incident() {
    echo "$*" | fabric -sp analyze_incident | nvim;
}
ft-analyze_interviewer_techniques() {
    echo "$*" | fabric -sp analyze_interviewer_techniques | nvim;
}
ft-analyze_logs() {
    echo "$*" | fabric -sp analyze_logs | nvim;
}
ft-analyze_malware() {
    echo "$*" | fabric -sp analyze_malware | nvim;
}
ft-analyze_paper() {
    echo "$*" | fabric -sp analyze_paper | nvim;
}
ft-analyze_patent() {
    echo "$*" | fabric -sp analyze_patent | nvim;
}
ft-analyze_personality() {
    echo "$*" | fabric -sp analyze_personality | nvim;
}
ft-analyze_presentation() {
    echo "$*" | fabric -sp analyze_presentation | nvim;
}
ft-analyze_product_feedback() {
    echo "$*" | fabric -sp analyze_product_feedback | nvim;
}
ft-analyze_prose() {
    echo "$*" | fabric -sp analyze_prose | nvim;
}
ft-analyze_prose_json() {
    echo "$*" | fabric -sp analyze_prose_json | nvim;
}
ft-analyze_prose_pinker() {
    echo "$*" | fabric -sp analyze_prose_pinker | nvim;
}
ft-analyze_sales_call() {
    echo "$*" | fabric -sp analyze_sales_call | nvim;
}
ft-analyze_spiritual_text() {
    echo "$*" | fabric -sp analyze_spiritual_text | nvim;
}
ft-analyze_tech_impact() {
    echo "$*" | fabric -sp analyze_tech_impact | nvim;
}
ft-analyze_threat_report() {
    echo "$*" | fabric -sp analyze_threat_report | nvim;
}
ft-analyze_threat_report_trends() {
    echo "$*" | fabric -sp analyze_threat_report_trends | nvim;
}
ft-answer_interview_question() {
    echo "$*" | fabric -sp answer_interview_question | nvim;
}
ft-ask_secure_by_design_questions() {
    echo "$*" | fabric -sp ask_secure_by_design_questions | nvim;
}
ft-capture_thinkers_work() {
    echo "$*" | fabric -sp capture_thinkers_work | nvim;
}
ft-check_agreement() {
    echo "$*" | fabric -sp check_agreement | nvim;
}
ft-clean_text() {
    echo "$*" | fabric -sp clean_text | nvim;
}
ft-coding_master() {
    echo "$*" | fabric -sp coding_master | nvim;
}
ft-compare_and_contrast() {
    echo "$*" | fabric -sp compare_and_contrast | nvim;
}
ft-create_5_sentence_summary() {
    echo "$*" | fabric -sp create_5_sentence_summary | nvim;
}
ft-create_academic_paper() {
    echo "$*" | fabric -sp create_academic_paper | nvim;
}
ft-create_ai_jobs_analysis() {
    echo "$*" | fabric -sp create_ai_jobs_analysis | nvim;
}
ft-create_aphorisms() {
    echo "$*" | fabric -sp create_aphorisms | nvim;
}
ft-create_art_prompt() {
    echo "$*" | fabric -sp create_art_prompt | nvim;
}
ft-create_better_frame() {
    echo "$*" | fabric -sp create_better_frame | nvim;
}
ft-create_coding_project() {
    echo "$*" | fabric -sp create_coding_project | nvim;
}
ft-create_command() {
    echo "$*" | fabric -sp create_command | nvim;
}
ft-create_cyber_summary() {
    echo "$*" | fabric -sp create_cyber_summary | nvim;
}
ft-create_formal_email() {
    echo "$*" | fabric -sp create_formal_email | nvim;
}
ft-create_git_diff_commit() {
    echo "$*" | fabric -sp create_git_diff_commit | nvim;
}
ft-create_graph_from_input() {
    echo "$*" | fabric -sp create_graph_from_input | nvim;
}
ft-create_hormozi_offer() {
    echo "$*" | fabric -sp create_hormozi_offer | nvim;
}
ft-create_idea_compass() {
    echo "$*" | fabric -sp create_idea_compass | nvim;
}
ft-create_investigation_visualization() {
    echo "$*" | fabric -sp create_investigation_visualization | nvim;
}
ft-create_keynote() {
    echo "$*" | fabric -sp create_keynote | nvim;
}
ft-create_logo() {
    echo "$*" | fabric -sp create_logo | nvim;
}
ft-create_markmap_visualization() {
    echo "$*" | fabric -sp create_markmap_visualization | nvim;
}
ft-create_mermaid_visualization() {
    echo "$*" | fabric -sp create_mermaid_visualization | nvim;
}
ft-create_mermaid_visualization_for_github() {
    echo "$*" | fabric -sp create_mermaid_visualization_for_github | nvim;
}
ft-create_micro_summary() {
    echo "$*" | fabric -sp create_micro_summary | nvim;
}
ft-create_network_threat_landscape() {
    echo "$*" | fabric -sp create_network_threat_landscape | nvim;
}
ft-create_npc() {
    echo "$*" | fabric -sp create_npc | nvim;
}
ft-create_pattern() {
    echo "$*" | fabric -sp create_pattern | nvim;
}
ft-create_quiz() {
    echo "$*" | fabric -sp create_quiz | nvim;
}
ft-create_reading_plan() {
    echo "$*" | fabric -sp create_reading_plan | nvim;
}
ft-create_recursive_outline() {
    echo "$*" | fabric -sp create_recursive_outline | nvim;
}
ft-create_report_finding() {
    echo "$*" | fabric -sp create_report_finding | nvim;
}
ft-create_rpg_summary() {
    echo "$*" | fabric -sp create_rpg_summary | nvim;
}
ft-create_security_update() {
    echo "$*" | fabric -sp create_security_update | nvim;
}
ft-create_show_intro() {
    echo "$*" | fabric -sp create_show_intro | nvim;
}
ft-create_sigma_rules() {
    echo "$*" | fabric -sp create_sigma_rules | nvim;
}
ft-create_story_explanation() {
    echo "$*" | fabric -sp create_story_explanation | nvim;
}
ft-create_stride_threat_model() {
    echo "$*" | fabric -sp create_stride_threat_model | nvim;
}
ft-create_summary() {
    echo "$*" | fabric -sp create_summary | nvim;
}
ft-create_tags() {
    echo "$*" | fabric -sp create_tags | nvim;
}
ft-create_threat_scenarios() {
    echo "$*" | fabric -sp create_threat_scenarios | nvim;
}
ft-create_ttrc_graph() {
    echo "$*" | fabric -sp create_ttrc_graph | nvim;
}
ft-create_ttrc_narrative() {
    echo "$*" | fabric -sp create_ttrc_narrative | nvim;
}
ft-create_upgrade_pack() {
    echo "$*" | fabric -sp create_upgrade_pack | nvim;
}
ft-create_video_chapters() {
    echo "$*" | fabric -sp create_video_chapters | nvim;
}
ft-create_visualization() {
    echo "$*" | fabric -sp create_visualization | nvim;
}
ft-explain_code() {
    echo "$*" | fabric -sp explain_code | nvim;
}
ft-explain_docs() {
    echo "$*" | fabric -sp explain_docs | nvim;
}
ft-explain_math() {
    echo "$*" | fabric -sp explain_math | nvim;
}
ft-explain_project() {
    echo "$*" | fabric -sp explain_project | nvim;
}
ft-explain_terms() {
    echo "$*" | fabric -sp explain_terms | nvim;
}
ft-export_data_as_csv() {
    echo "$*" | fabric -sp export_data_as_csv | nvim;
}
ft-extract_algorithm_update_recommendations() {
    echo "$*" | fabric -sp extract_algorithm_update_recommendations | nvim;
}
ft-extract_article_wisdom() {
    echo "$*" | fabric -sp extract_article_wisdom | nvim;
}
ft-extract_book_ideas() {
    echo "$*" | fabric -sp extract_book_ideas | nvim;
}
ft-extract_book_recommendations() {
    echo "$*" | fabric -sp extract_book_recommendations | nvim;
}
ft-extract_business_ideas() {
    echo "$*" | fabric -sp extract_business_ideas | nvim;
}
ft-extract_controversial_ideas() {
    echo "$*" | fabric -sp extract_controversial_ideas | nvim;
}
ft-extract_ctf_writeup() {
    echo "$*" | fabric -sp extract_ctf_writeup | nvim;
}
ft-extract_extraordinary_claims() {
    echo "$*" | fabric -sp extract_extraordinary_claims | nvim;
}
ft-extract_ideas() {
    echo "$*" | fabric -sp extract_ideas | nvim;
}
ft-extract_insights() {
    echo "$*" | fabric -sp extract_insights | nvim;
}
ft-extract_insights_dm() {
    echo "$*" | fabric -sp extract_insights_dm | nvim;
}
ft-extract_instructions() {
    echo "$*" | fabric -sp extract_instructions | nvim;
}
ft-extract_jokes() {
    echo "$*" | fabric -sp extract_jokes | nvim;
}
ft-extract_main_idea() {
    echo "$*" | fabric -sp extract_main_idea | nvim;
}
ft-extract_patterns() {
    echo "$*" | fabric -sp extract_patterns | nvim;
}
ft-extract_poc() {
    echo "$*" | fabric -sp extract_poc | nvim;
}
ft-extract_predictions() {
    echo "$*" | fabric -sp extract_predictions | nvim;
}
ft-extract_primary_problem() {
    echo "$*" | fabric -sp extract_primary_problem | nvim;
}
ft-extract_questions() {
    echo "$*" | fabric -sp extract_questions | nvim;
}
ft-extract_recommendations() {
    echo "$*" | fabric -sp extract_recommendations | nvim;
}
ft-extract_references() {
    echo "$*" | fabric -sp extract_references | nvim;
}
ft-extract_skills() {
    echo "$*" | fabric -sp extract_skills | nvim;
}
ft-extract_song_meaning() {
    echo "$*" | fabric -sp extract_song_meaning | nvim;
}
ft-extract_sponsors() {
    echo "$*" | fabric -sp extract_sponsors | nvim;
}
ft-extract_videoid() {
    echo "$*" | fabric -sp extract_videoid | nvim;
}
ft-extract_wisdom() {
    echo "$*" | fabric -sp extract_wisdom | nvim;
}
ft-extract_wisdom_agents() {
    echo "$*" | fabric -sp extract_wisdom_agents | nvim;
}
ft-extract_wisdom_dm() {
    echo "$*" | fabric -sp extract_wisdom_dm | nvim;
}
ft-extract_wisdom_nometa() {
    echo "$*" | fabric -sp extract_wisdom_nometa | nvim;
}
ft-find_hidden_message() {
    echo "$*" | fabric -sp find_hidden_message | nvim;
}
ft-find_logical_fallacies() {
    echo "$*" | fabric -sp find_logical_fallacies | nvim;
}
ft-get_wow_per_minute() {
    echo "$*" | fabric -sp get_wow_per_minute | nvim;
}
ft-get_youtube_rss() {
    echo "$*" | fabric -sp get_youtube_rss | nvim;
}
ft-improve_academic_writing() {
    echo "$*" | fabric -sp improve_academic_writing | nvim;
}
ft-improve_prompt() {
    echo "$*" | fabric -sp improve_prompt | nvim;
}
ft-improve_report_finding() {
    echo "$*" | fabric -sp improve_report_finding | nvim;
}
ft-improve_writing() {
    echo "$*" | fabric -sp improve_writing | nvim;
}
ft-label_and_rate() {
    echo "$*" | fabric -sp label_and_rate | nvim;
}
ft-official_pattern_template() {
    echo "$*" | fabric -sp official_pattern_template | nvim;
}
ft-provide_guidance() {
    echo "$*" | fabric -sp provide_guidance | nvim;
}
ft-rate_ai_response() {
    echo "$*" | fabric -sp rate_ai_response | nvim;
}
ft-rate_ai_result() {
    echo "$*" | fabric -sp rate_ai_result | nvim;
}
ft-rate_content() {
    echo "$*" | fabric -sp rate_content | nvim;
}
ft-rate_value() {
    echo "$*" | fabric -sp rate_value | nvim;
}
ft-raw_query() {
    echo "$*" | fabric -sp raw_query | nvim;
}
ft-raycast() {
    echo "$*" | fabric -sp raycast | nvim;
}
ft-recommend_artists() {
    echo "$*" | fabric -sp recommend_artists | nvim;
}
ft-recommend_pipeline_upgrades() {
    echo "$*" | fabric -sp recommend_pipeline_upgrades | nvim;
}
ft-recommend_talkpanel_topics() {
    echo "$*" | fabric -sp recommend_talkpanel_topics | nvim;
}
ft-show_fabric_options_markmap() {
    echo "$*" | fabric -sp show_fabric_options_markmap | nvim;
}
ft-solve_with_cot() {
    echo "$*" | fabric -sp solve_with_cot | nvim;
}
ft-suggest_pattern() {
    echo "$*" | fabric -sp suggest_pattern | nvim;
}
ft-summarize() {
    echo "$*" | fabric -sp summarize | nvim;
}
ft-summarize_debate() {
    echo "$*" | fabric -sp summarize_debate | nvim;
}
ft-summarize_git_changes() {
    echo "$*" | fabric -sp summarize_git_changes | nvim;
}
ft-summarize_git_diff() {
    echo "$*" | fabric -sp summarize_git_diff | nvim;
}
ft-summarize_lecture() {
    echo "$*" | fabric -sp summarize_lecture | nvim;
}
ft-summarize_legislation() {
    echo "$*" | fabric -sp summarize_legislation | nvim;
}
ft-summarize_micro() {
    echo "$*" | fabric -sp summarize_micro | nvim;
}
ft-summarize_newsletter() {
    echo "$*" | fabric -sp summarize_newsletter | nvim;
}
ft-summarize_paper() {
    echo "$*" | fabric -sp summarize_paper | nvim;
}
ft-summarize_prompt() {
    echo "$*" | fabric -sp summarize_prompt | nvim;
}
ft-summarize_pull-requests() {
    echo "$*" | fabric -sp summarize_pull | nvim;
}
ft-summarize_rpg_session() {
    echo "$*" | fabric -sp summarize_rpg_session | nvim;
}
ft-to_flashcards() {
    echo "$*" | fabric -sp to_flashcards | nvim;
}
ft-transcribe_minutes() {
    echo "$*" | fabric -sp transcribe_minutes | nvim;
}
ft-tweet() {
    echo "$*" | fabric -sp tweet | nvim;
}
ft-write_essay() {
    echo "$*" | fabric -sp write_essay | nvim;
}
ft-write_hackerone_report() {
    echo "$*" | fabric -sp write_hackerone_report | nvim;
}
ft-write_latex() {
    echo "$*" | fabric -sp write_latex | nvim;
}
ft-write_micro_essay() {
    echo "$*" | fabric -sp write_micro_essay | nvim;
}
ft-write_nuclei_template_rule() {
    echo "$*" | fabric -sp write_nuclei_template_rule | nvim;
}
ft-write_pull-request() {
    echo "$*" | fabric -sp write_pull | nvim;
}
ft-write_semgrep_rule() {
    echo "$*" | fabric -sp write_semgrep_rule | nvim;
}

