# change the keyboard layout -> by khalid mesbah
super + space 
  ~/repos/workflow/scripts/toggleKbd.sh
F7
  ~/repos/workflow/scripts/toggleKbd.sh
alt + Tab
  notify-send "alt+tab"
XF86AudioRaiseVolume
  source ~/repos/workflow/scripts/volumeIndicator.sh "up"
XF86AudioLowerVolume
  ~/repos/workflow/scripts/volumeIndicator.sh "down"
XF86AudioMute
  ~/repos/workflow/scripts/volumeIndicator.sh "mute"

#pactl set-sink-volume @DEFAULT_SINK@ -5%
#pactl set-sink-mute @DEFAULT_SINK@ toggle
#pactl set-sink-volume @DEFAULT_SINK@ +5%
# bindsym XF86AudioRaiseVolume exec  && $refresh_i3status && exec ~/scripts/volumeIndicator.sh "up"
# bindsym XF86AudioLowerVolume exec $refresh_i3status && exec ~/scripts/volumeIndicator.sh "down" 
# bindsym XF86AudioMute exec $refresh_i3status && exec ~/scripts/volumeIndicator.sh "mute"  
# b
